const Discord = require("discord.js");
const config = require("../Configuration/config.json");

module.exports = async(bot, msg, args) => {
    msg.channel.send('se vuoi assistenza dallo staff vai in caffetteria o apri un ticket ti consiglio anche di leggere #☕info-caffetteria'); 
}